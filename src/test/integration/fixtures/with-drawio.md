# Drawio fixture

This document is used by the integration test for the inline drawio
viewer. The link below is a paragraph-only `.drawio` reference, so the
collab editor should promote it to an inline rendered diagram.

[Flow diagram](diagrams/flow.drawio)

The link in this paragraph mixes with text — [the same diagram](diagrams/flow.drawio)
appears alongside other words. Mixed-text paragraphs should NOT be promoted.
